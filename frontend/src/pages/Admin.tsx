import {FormEvent,useEffect,useState} from 'react';import {users,err} from '../api';import {Button,Field,PageHeader,Status,Toolbar,Empty} from '../components/UI';import type {User} from '../types/api';
export default function Admin(){const [rows,setRows]=useState<User[]>([]),[form,setForm]=useState(false),[q,setQ]=useState(''),[e,setE]=useState('');const load=()=>users.list().then(r=>setRows(r.data)).catch(x=>setE(err(x)));useEffect(()=>{ load(); },[]);if(form)return <UserForm onDone={()=>{setForm(false);load()}}/>;return <><PageHeader title="Users & Access" subtitle="Create and manage application users and their roles." action={<Button onClick={()=>setForm(true)}>＋ Create User</Button>}/><Toolbar search={q} onSearch={setQ} refresh={load}/><div className="table-card">{rows.filter(x=>JSON.stringify(x).toLowerCase().includes(q.toLowerCase())).length?<table><thead><tr><th>Name</th><th>Login Id</th><th>Email</th><th>Role</th><th>Status</th></tr></thead><tbody>{rows.filter(x=>JSON.stringify(x).toLowerCase().includes(q.toLowerCase())).map(u=><tr key={u.id}><td><b>{u.name}</b></td><td>{u.login_id}</td><td>{u.email}</td><td>{u.role}</td><td><Status value={u.is_active?'Active':'Inactive'}/></td></tr>)}</tbody></table>:<Empty text="No users found"/>}</div>{e&&<div className="error">{e}</div>}</>}
function UserForm({onDone}:{onDone:()=>void}){const [f,setF]=useState<any>({name:'',login_id:'',email:'',role:'Accountant',password:'',re_enter_password:''}),[e,setE]=useState('');const submit=async(x:FormEvent)=>{x.preventDefault();if(f.password!==f.re_enter_password)return setE('Passwords do not match');try{const payload={name:f.name,login_id:f.login_id,email:f.email,role:f.role,password:f.password,re_password:f.re_enter_password||f.password};await users.create(payload);onDone()}catch(x){setE(err(x))}};return <div className="modal-overlay" role="dialog" aria-modal="true"><div className="form-modal"><PageHeader title="Create User" subtitle="Administrator can create Accountant or Contact User accounts." back={onDone}/><form className="record-form" onSubmit={submit}><div className="form-grid"><Field label="Name" required><input value={f.name} onChange={e=>setF({...f,name:e.target.value})}/></Field><Field label="Login Id" required><input minLength={6} maxLength={12} value={f.login_id} onChange={e=>setF({...f,login_id:e.target.value})}/></Field><Field label="Email Id" required><input type="email" value={f.email} onChange={e=>setF({...f,email:e.target.value})}/></Field><Field label="Role" required><select value={f.role} onChange={e=>setF({...f,role:e.target.value})}><option value="User">Contact User</option><option value="Accountant">Accountant</option></select></Field><Field label="Password" required><input type="password" value={f.password} onChange={e=>setF({...f,password:e.target.value})}/></Field><Field label="Re-enter Password" required><input type="password" value={f.re_enter_password} onChange={e=>setF({...f,re_enter_password:e.target.value})}/></Field></div>{e&&<div className="error">{e}</div>}<div className="form-actions"><Button type="submit">CREATE</Button><Button type="button" variant="secondary" onClick={onDone}>CANCEL</Button></div></form></div></div>}
